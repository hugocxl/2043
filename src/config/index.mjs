'use strict'

import { SCALE_UNIT } from '../constants/index.mjs'

export const config = {
  gameFPS: 1000 / 60,
  viewPointHeight: 50 * SCALE_UNIT,
  levelDuration: 10000,
  timeBetweenLevels: 5000,
  itemsLimit: 500
}
