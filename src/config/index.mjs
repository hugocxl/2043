'use strict'

import { SCALE_UNIT } from '../constants/index.mjs'

export const config = {
  gameFPS: 1000 / 60,
  viewPointHeight: 5 * SCALE_UNIT,
  levelDuration: 10000,
  timeBetweenLevels: 0,
  itemsLimit: 500
}
